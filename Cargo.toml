[package]
name = "air-mouse"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
enigo = { version = "0.2", features = ["xdo"] }
evdev = "0.12"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = 3
lto = true
